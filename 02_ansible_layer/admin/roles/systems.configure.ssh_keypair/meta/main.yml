---
galaxy_info:
  role_name: systems_configure_ssh_keypair
  namespace: range42
  author: range42
  description: Deploy an SSH keypair into a user's ~/.ssh (private + optional public).
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: Ubuntu
      versions:
        - noble
dependencies: []
