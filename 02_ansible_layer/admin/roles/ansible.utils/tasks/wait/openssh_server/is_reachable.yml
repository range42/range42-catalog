---
# Use the inventory connection, including ProxyJump, SSH keys and known_hosts.
# Calling the ssh binary with inventory_hostname required a separate global
# SSH alias and could silently continue after exhausting its retries.
- name: Wait for the guest connection
  ansible.builtin.wait_for_connection:
    connect_timeout: 5
    sleep: 5
    timeout: 300
