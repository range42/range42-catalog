---

- name: configure - deploy acng.conf
  ansible.builtin.template:
    src: acng.conf.j2
    dest: /etc/apt-cacher-ng/acng.conf
    owner: root
    group: root
    mode: "0644"
  become: true
  notify: restart apt-cacher-ng
