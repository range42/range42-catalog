CacheDir: /var/cache/apt-cacher-ng
LogDir: /var/log/apt-cacher-ng
Port: 3142

# Expire cached files older than this (days). 0 = keep forever.
ExThreshold: 30

# Allow requests from these networks. Ansible template overrides this.
AllowedClients: 0.0.0.0/0

# Passthrough HTTPS (signed repos, PPAs) directly to upstream.
PassThroughPattern: .*

# Report page
ReportPage: acng-report.html

# Remap entries for Debian and Ubuntu
Remap-debrep: file:deb_mirror*.gz /debian ; http://deb.debian.org/debian
Remap-uburep: file:ubuntu_mirrors /ubuntu ; http://archive.ubuntu.com/ubuntu
Remap-debsec: file:deb_mirror*.gz /debian-security ; http://deb.debian.org/debian-security
